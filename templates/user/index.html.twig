{% extends 'base.html.twig' %}


{% block body %}

  <section>

    <h1>Hello {{ user.name }}!</h1>
    
    <P>{{user.email}}</P>

    </section>
    <section>

      <h2>Mes sacs</h2>

      {% for ownerbag in ownerbags %}

      <p>{{ ownerbag.name}}</p>
        
      {% endfor %}
      

    </section>

{% endblock %}